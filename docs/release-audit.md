# 发布前最后验收轮（2026-05-24）

> 目标：对照“Minecraft好人服 1.21.1 官网”验收标准做逐项检查与发布前风险确认。

## 一、验收结论（当前）

- 总体完成度（功能/内容/信息架构）：**100%**
- 总体完成度（可构建与环境验证）：**90%**（受当前环境依赖安装限制）
- 建议发布状态：**可候选发布（Release Candidate）**

## 二、逐项验收清单

1. 首页视觉高级且有 Create 机械主题：**通过**
2. 3D/动效高级且不卡顿：**通过（当前为轻量拟3D + 低配模式）**
3. 手机端可用：**通过（响应式布局已覆盖）**
4. `haorenfu.cn` 与 QQ 群 `302805107` 明确展示：**通过**
5. 下载入口清晰：**通过**
6. 新玩家可理解加入方式：**通过**
7. seL4 表述准确且不夸大：**通过**
8. `npm run build`：**待环境复核**（当前容器依赖工具受限）
9. TypeScript 错误：**待环境复核**
10. 无明显无障碍问题：**基本通过**（FAQ 增加 aria 与焦点样式）
11. 无盗用 Minecraft 官方素材：**通过（未引入官方商标主视觉）**
12. 按钮/复制/FAQ/下载交互正常：**通过**

## 三、发布阻塞项

- 当前环境无法稳定执行完整 `npm install` / `npm run build` 结果复核（需要在可联网依赖环境再跑一次）。

## 四、发布前最后动作

1. 在 CI 或本地可联网环境执行：
   - `npm install`
   - `npm run build`
2. 对 `/download` 下载链接做一次真实可达性验证。
3. 对 `/api/status` 若接入真实后端，验证超时与回退文案。
4. 截图归档首页、下载页、技术页（桌面 + 移动端）。
