import type{Metadata}from"next";import{PageHero}from"@/components/site/page-hero";import{ModBrowser}from"./mod-browser";import{excludedMods,verifiedShaders}from"@/lib/mods";export const metadata:Metadata={title:"模组百科"};export default function Page(){return <main id="main-content"><PageHero eyebrow="AUDITED MOD ENCYCLOPEDIA" title="56 个组件，各有自己的位置。" description="这里列出 1.1.0 可审计基线中的 56 个组件；1.2.0 完整原包取得后应重新扫描并更新。"/><section className="section"><div className="site-shell"><div className="notice-box">版本边界：精确版本来自 1.1.0 锁文件，不能冒充缺少原始 MRPACK 的 1.2.0 完整清单。</div><ModBrowser/></div></section><section className="section alt"><div className="site-shell two-column"><div><h2>已核验光影</h2>{verifiedShaders.map(x=><article className="reference-item" key={x.name}><strong>{x.name}</strong><span>{x.version} · {x.use}</span></article>)}</div><div><h2>有意不纳入</h2>{excludedMods.map(x=><article className="reference-item" key={x.name}><strong>{x.name}</strong><span>{x.reason}</span></article>)}</div></div></section></main>}
