import type{Metadata}from"next";import{PageHero}from"@/components/site/page-hero";import{gameplayPillars}from"@/lib/site-data";export const metadata:Metadata={title:"世界玩法"};export default function Page(){return <main id="main-content"><PageHero eyebrow="WORLD SYSTEMS" title="八条玩法路线，在长期世界彼此交汇。" description="从航空、轨道和工业到群星、东方幻想与文化创作，每条路线都可以独立开始，也会在大型工程里互相连接。"/><section className="section"><div className="site-shell chapter-list">{gameplayPillars.map(p=><article id={p.slug} className="chapter" key={p.slug}><span>{p.index}</span><div><p className="eyebrow">{p.difficulty} · {p.crew} · {p.start}</p><h2>{p.title}</h2><h3>{p.kicker}</h3><p>{p.description}</p><div className="tags">{p.mods.map(m=><span key={m}>{m}</span>)}</div><ol>{p.steps.map(s=><li key={s}>{s}</li>)}</ol></div></article>)}</div></section></main>}
